build:
  image: toolkt/odoo:odoo_rancher
  commands:
    - ls -la

compose:
  postgres:
    image: postgres:9.6

cache:
  mount:
    - .git

publish:
  docker:
    storage_driver: overlay
    username: agpontejos
    password: f3b191980
    email: agpontejos@gmail.com
    repo: toolkt/odoo
    tag: odoo_rancher